import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute(
  '/_layout/accl/investor-relation-documents/add/',
)({
  component: () => (
    <div>Hello /_layout/accl/investor-relation-documents/add/!</div>
  ),
})
