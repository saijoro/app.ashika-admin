import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute(
  '/_layout/accl-reports/investor-relation-documents',
)({
  component: () => (
    <div>Hello /_layout/accl-reports/investor-relation-documents!</div>
  ),
})
